name: Diputación teoría v87 bloque 4

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  diputacion-theory-b4:
    name: Diputación · temas 31 y 32
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Check syntax
        run: |
          node --check assets/js/diputacion-teoria-v87-bloque4a.js
          node --check assets/js/diputacion-teoria-v87-bloque4b.js
          node --check tests/diputacion-teoria-v87-bloque4.js
      - name: Validate theory, sources and numbering
        run: node tests/diputacion-teoria-v87-bloque4.js
      - name: Upload audit
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: diputacion-teoria-v87-bloque4
          path: diputacion-teoria-v87-bloque4.json
          if-no-files-found: ignore
          retention-days: 30
